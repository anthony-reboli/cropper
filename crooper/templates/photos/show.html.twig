{% extends 'base.html.twig' %}

{% block title %}Photos{% endblock %}

{% block body %}
    <h1>Photos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ photo.nom }}</td>
            </tr>
            <tr>
                <th>Type_mine</th>
                <td>{{ photo.typeMine }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('photos_index') }}">back to list</a>

    <a href="{{ path('photos_edit', {'id': photo.id}) }}">edit</a>

    {{ include('photos/_delete_form.html.twig') }}
{% endblock %}
